# МедАнализ — система анализа рентгеновских снимков

`МедАнализ` — это настольное приложение на **Python + PyQt6** для загрузки, просмотра и базового анализа рентгеновских снимков с использованием обученной модели **TensorFlow / Keras**. Приложение позволяет выбрать пациента, загрузить снимок, запустить анализ и сохранить текстовое заключение.

## Возможности

- Просмотр списка пациентов.
- Поиск и фильтрация пациентов по статусу.
- Открытие карточки пациента.
- Загрузка рентгеновского снимка с компьютера.
- Отображение истории снимков.
- Имитация процесса анализа с прогресс-баром.
- Анализ снимка через модель `best_model.keras`.
- Вывод результата: перелом обнаружен / не обнаружен.
- Генерация текстового заключения.
- Сохранение заключения в `.txt` файл.

## Стек технологий

- **Python 3**
- **PyQt6** — графический интерфейс
- **TensorFlow / Keras** — загрузка и использование модели
- **NumPy** — работа с массивами
- **Pillow (PIL)** — загрузка и предобработка изображений

## Структура приложения

Приложение состоит из двух основных классов:

### `PatientCard`
Карточка пациента, которая отображает:
- имя;
- возраст;
- ID;
- диагноз;
- статус;
- кнопку выбора пациента.

### `MedicalApp`
Главное окно приложения, содержащее:
- вкладку со списком пациентов;
- вкладку карточки пациента и анализа снимка;
- вкладку результатов анализа.

## Логика работы

1. Пользователь запускает приложение.
2. Открывается список пациентов.
3. После выбора пациента становится доступна вкладка анализа.
4. Пользователь загружает рентгеновский снимок.
5. После нажатия кнопки запускается анализ.
6. Изображение приводится к размеру `224x224`, конвертируется в `RGB`, нормализуется и передаётся в модель.
7. Модель возвращает вероятность наличия перелома.
8. На экране отображается результат и формируется текстовое заключение.
9. Заключение можно сохранить в текстовый файл.

## Предобработка изображения

Перед подачей в модель изображение проходит следующие шаги:

- открытие через `PIL.Image`;
- изменение размера до `224x224`;
- преобразование в `RGB`;
- перевод в массив `NumPy`;
- нормализация значений пикселей в диапазон от `0` до `1`;
- добавление размерности batch.

Пример из кода:

```python
img = Image.open(self.current_image_path).resize((224, 224)).convert('RGB')
img_array = np.array(img, dtype=np.float32) / 255.0
img_array = np.expand_dims(img_array, 0)
pred = self.model.predict(img_array, verbose=0)[0][0]
```

## Интерфейс

### Вкладка «Список пациентов»
- поиск по имени и ID;
- фильтр по статусу;
- карточки пациентов в сетке.

### Вкладка «Карточка пациента»
- сведения о выбранном пациенте;
- область загрузки снимка;
- история снимков;
- дата исследования;
- поле комментариев;
- запуск анализа.

### Вкладка «Результаты анализа»
- отображение снимка;
- итоговый статус;
- описание результата;
- подробное заключение;
- сохранение отчёта.

## Установка

### 1. Клонировать проект

```bash
git clone <repo-url>
cd <project-folder>
```

### 2. Создать виртуальное окружение

```bash
python -m venv venv
```

#### Windows
```bash
venv\Scripts\activate
```

#### Linux / macOS
```bash
source venv/bin/activate
```

### 3. Установить зависимости

```bash
pip install pyqt6 tensorflow numpy pillow
```

## Запуск

```bash
python main.py
```

> Если файл приложения называется иначе, замени `main.py` на имя твоего файла.

## Модель

В коде модель загружается по абсолютному пути:

```python
self.model = keras.models.load_model(r'D:\vkladki\best_model.keras', compile=False)
```

Для переноса проекта на другой компьютер лучше заменить путь на относительный, например:

```python
self.model = keras.models.load_model('best_model.keras', compile=False)
```

И положить файл модели рядом с приложением.

## Формат результата

После анализа приложение показывает один из двух статусов:

- **Обнаружен перелом**
- **Переломов не обнаружено**

Также рассчитывается условная уверенность предсказания в процентах.

## Сохранение отчёта

Отчёт сохраняется в `.txt` и содержит:

- имя пациента;
- итог анализа;
- краткое описание;
- комментарии медицинского работника.

## Известные ограничения

- В проекте используются тестовые данные пациентов, а не база данных.
- История снимков сейчас демонстрационная.
- Прогресс анализа визуально имитируется через `QTimer`.
- Качество результата полностью зависит от обученной модели `best_model.keras`.
- В коде нет отдельной обработки drag-and-drop, хотя это указано в интерфейсе.
- Абсолютный путь к модели делает проект менее переносимым.



## Пример структуры проекта

```text
project/
├── main.py
├── best_model.keras
├── README.md
└── assets/
```

